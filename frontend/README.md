# Wood Stone Corbett - Frontend (MVP)

Quick start:

```powershell
cd "frontend"
npm install
npm run dev
```

The frontend uses Vite + React + Tailwind. It is configured to call the backend on `http://localhost:5000` by default.
